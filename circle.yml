machine:
  java:
    version: oraclejdk8
  services:
    - docker

dependencies:
  override:
    - docker info
    - add-apt-repository ppa:cwchien/gradle
    - apt-get update
    - apt-get install gradle
    - gradle wrapper
    - ./gradlew build buildDocker

test:
  override:
    - ./gradlew build test